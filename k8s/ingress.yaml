apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-core-v2-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-core-v2.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-core-v2-service
            port:
              number: 80
